#!/usr/bin/env bash
set -euo pipefail

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "pre-push: gitleaks is required but not installed"
  exit 1
fi

bash scripts/boundary-scan.sh
gitleaks detect
